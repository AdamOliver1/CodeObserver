import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    title: {
        margin: '5%'
    },
    subtitle: {
        marginTop: '10%',
        marginBottom: '2%'
    }
});

export default useStyles;